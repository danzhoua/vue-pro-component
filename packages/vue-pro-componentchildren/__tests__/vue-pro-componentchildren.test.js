'use strict';

const vueProComponentchildren = require('..');
const assert = require('assert').strict;

assert.strictEqual(vueProComponentchildren(), 'Hello from vueProComponentchildren');
console.info("vueProComponentchildren tests passed");
